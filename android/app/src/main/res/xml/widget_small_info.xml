<?xml version="1.0" encoding="utf-8"?>
<!-- WID-1: the small widget. updatePeriodMillis is a backstop only; the
     provider sets an alarm for the exact moment the numbers change, and this
     puts one back after a reboot, when alarms are cleared (WID-5). -->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:description="@string/widget_small_description"
    android:initialLayout="@layout/widget_small"
    android:minHeight="110dp"
    android:minWidth="110dp"
    android:previewImage="@mipmap/ic_launcher"
    android:previewLayout="@layout/widget_small"
    android:resizeMode="horizontal|vertical"
    android:targetCellHeight="2"
    android:targetCellWidth="2"
    android:updatePeriodMillis="1800000"
    android:widgetCategory="home_screen" />
