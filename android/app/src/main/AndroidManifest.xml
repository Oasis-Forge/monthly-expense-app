<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- The one network permission the app has, and it is the ad slots that
         need it (RUN-2, ADS-6). Nothing the user records is ever sent
         anywhere: entry, budgets, notes, reports, attachments and backups are
         all local, and the ad SDK is handed none of it (ADS-7). -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Reminders are rescheduled after a reboot, so the note due tomorrow
         still arrives if the phone is restarted tonight (NUDGE-9, NOTE-6). -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <!-- BAK-8: the phone's own automatic backup is off, so Android never
         copies the records to Google Drive behind the user's back. -->
    <application
        android:label="Monthly Expenses"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- The home-screen widgets (WID-1). They draw only what the app has
             handed them and reach nothing else, so neither is exported. -->
        <receiver
            android:name=".SmallExpenseWidget"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_small_info" />
        </receiver>
        <receiver
            android:name=".MediumExpenseWidget"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_medium_info" />
        </receiver>
        <!-- flutter_local_notifications ships these three but declares none
             of them: its own manifest carries two permissions and nothing
             else, so an app that does not declare them here schedules alarms
             whose PendingIntent points at a component that does not exist.
             Android then fires the alarm on time, the broadcast resolves to
             nothing, and no notification is ever posted — silently, with no
             log and no error. That is what kept every reminder from arriving
             (NOTE-6, NUDGE-1). None is exported: they are ours alone. -->
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver"
            android:exported="false"/>
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ActionBroadcastReceiver"
            android:exported="false"/>
        <!-- Reminders are re-laid after a restart or an app update, or they
             would be lost with the alarms the reboot cleared (NUDGE-9).
             Known gap (pr59_10): the plugin's receiver re-arms a missed
             reminder at boot even when it is more than two hours overdue.
             Dropping it would need a receiver of our own over the plugin's
             private cache, so it is left for after launch. -->
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>
        <!-- The AdMob app this build belongs to ("Monthly expense Tracker
             android"). The SDK reads it before Dart runs, so it cannot come
             from `AdsConfig`; it must match `liveAppIdAndroid` in
             `lib/services/ads_config.dart`, and a test fails if it doesn't.
             A wrong ID here crashes the app at start-up. -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-8287765177319119~2977310217" />
        <!-- Without this, the SDK starts sending app-measurement events to
             Google as soon as the process starts, before Dart ever calls
             MobileAds.instance.initialize() — which AdsProvider gates on
             setup, the walkthrough and consent (ADS-4, ADS-5), and never
             calls at all once "Remove ads" is bought (ADS-8, PAY-1). -->
        <meta-data
            android:name="com.google.android.gms.ads.DELAY_APP_MEASUREMENT_INIT"
            android:value="true" />
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
